services:
  shell:
    build: .
    entrypoint:
      - /bin/sh
    image: ghcr.io/williamjacksn/aws-scripts
    init: true
    volumes:
      - ./:/home/python/aws-scripts
    working_dir: /home/python/aws-scripts
